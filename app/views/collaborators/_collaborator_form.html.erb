<%= form_tag(:controller => :collaborators,
							:action => :create,
							:project_id => project_id,
							:html => {:class => 'form-vertical' }) do |f| %> 
  <%= text_field_tag :email %>  
  
  <%= submit_tag('Add Collaborator', :class => 'btn-primary') %>
<% end %>