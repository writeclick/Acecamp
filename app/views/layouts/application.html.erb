<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "DBC-Camp" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "App_Name" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
    </header>
    <div id="main" role="main">
      <div class="container">
        <div class="content">
          <div class="row">
            <div class="span8">
              <div class="well">
                <%= yield %>
              </div>
            </div>
            <div class="span4">
              <div class="well">
                <h5>Placeholder Heading</h5>
              </div>
            </div>
          </div>
        </div> <!-- end of .content -->
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
    <footer>
      <div class="container">
        Nothing Here Yet...
      </div>
    </footer>
  </body>
</html>