<%= form_tag(:controller => "projects", :action => "create", :method => "post", :html => {:class => 'form-vertical' }) do |f| %> 
  <%= text_field :project, :title %> 
  <%= text_area :project, :description %>
  <%= submit_tag("Create Project", :class => 'btn-primary') %>
<% end %>