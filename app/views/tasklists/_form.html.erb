<%= form_tag(:controller => :tasklists,
							:action => :create,
							:project_id => project_id,
							:html => {:class => 'form-vertical' }) do |f| %> 
  <%= text_field :tasklist, :name %>  
  
  <%= submit_tag('Add Tasklist', :class => 'btn-primary') %>
<% end %>