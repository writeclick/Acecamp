<%= form_tag(:controller => "tasks", 
						 :action => "create", 
						 :method => "post", 
						 :tasklist_id => tasklist_id,
						 :html => {:class => 'form-vertical' }) do |f| %> 
  <%= text_field :task, :name %>  
  <%= text_area :task, :description %>  
  
  <%= submit_tag('Add Task', :class => 'btn-primary') %>
<% end %>